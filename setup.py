"""Setup script for editable installs.

This minimal setup.py enables `pip install -e .` for development workflow.
All package metadata and dependencies are defined in pyproject.toml (PEP 621).
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
